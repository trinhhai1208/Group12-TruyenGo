<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ui.main.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TruyenGo"
        android:usesCleartextTraffic="true">

        <!-- Màn hình Đăng nhập (Chạy đầu tiên) -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.ForgotPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".ui.account.AccountManagementActivity"
            android:exported="false" />

        <activity
            android:name=".ui.account.AppInfoActivity"
            android:exported="false" />

        <activity
            android:name=".ui.account.ChangePasswordActivity"
            android:exported="false" />

        <activity
            android:name=".ui.book.BookListActivity"
            android:exported="false" />

        <activity
            android:name=".ui.book.BookDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.book.ReadActivity"
            android:exported="false" />

    </application>

    <uses-permission
        android:name="android.permission.INTERNET"
        tools:ignore="ManifestOrder" />

</manifest>